import 'package:flutter/material.dart';

class gatheringinfo extends StatelessWidget {
  const gatheringinfo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("info screen"),
    );
  }
}
